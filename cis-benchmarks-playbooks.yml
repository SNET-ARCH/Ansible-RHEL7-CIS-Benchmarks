- hosts: localhost
  connection: local
  gather_facts: true
  become: yes
  tasks:
  
    - name: Include a play after another play
      include_tasks: 1.1.1.1.yml
      static: no

   
  
#  roles:
#    - Ansible-RHEL7-CIS-Benchmarks 

 
#  tasks:
#    - include: roles/Ansible-RHEL7-CIS-Benchmarks/tasks/level-1/"{{ hostvar }}.yml"
#    - include_role:
#        name: 
#        tasks_from: level-1
#        with_items:
#          - "{{ hostvar }}.yml"


#  roles:
#    - Ansible-RHEL7-CIS-Benchmarks 
#  tasks:
#    - include_tasks: "{{ item }}"
#       with_fileglob: 
    
    
    
#    - include: "{{ item }}"
#  with_fileglob:
#    - roles/Ansible-RHEL7-CIS-Benchmarks/tasks/ path/to/your/tasks/*.yml
